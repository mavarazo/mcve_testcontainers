server:
  port: 8003

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: true
  jpa:
    hibernate.ddl-auto: none
    open-in-view: false
  main:
    banner-mode: off
  threads:
    virtual:
      enabled: true
